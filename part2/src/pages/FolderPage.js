import { useEffect, useState } from "react";
import { getFolderInfo } from "../api";
import Folder from "../components/Folder/Folder";

function FolderPage() {
  return <>{/* <MainContent links={folderInfo.links} /> */}</>;
}

export default FolderPage;
